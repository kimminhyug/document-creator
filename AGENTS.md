# 작업 기준

- 기존 `rules/` 가이드는 사용자가 합의한 내용이다. 의미 변경 시 변경 이유를 기록한다.
- 내용을 모르면 `pending`과 담당자를 남긴다. 예시 데이터를 실제 사실로 바꾸지 않는다.
- 디자인과 업무 내용을 분리한다. 프로젝트별 값을 생성기 코드에 하드코딩하지 않는다.
- 외부 서비스 전송·배포·새 계정 사용을 작업의 기본 단계로 추가하지 않는다.
- 새 런타임·폰트·라이브러리를 임의 설치하거나 폰트 파일을 저장소에 배포하지 않는다.
- 테스트와 실제 출력 검증을 구분한다. PDF가 Word의 시각 검증을 대신하지 않는다.
- 결과는 새 실행 폴더에 기록하고 기존 문서를 덮어쓰지 않는다.
- 변경과 검증·제약은 `docs/development/STATUS.md`에 기록한다.
